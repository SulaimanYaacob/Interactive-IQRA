export enum ROLE {
  TUTOR = "TUTOR",
  ADMIN = "ADMIN",
}

//! PrismaClient is too big, create your own enum instead.
export enum STATUS {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  REJECTED = "REJECTED",
}
